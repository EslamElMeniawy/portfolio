import { Screen } from "@src/components";
import * as React from "react";
import { Text } from "react-native-paper";

export default React.memo(() => (
  <Screen>
    <Text variant="titleLarge">Portfolio Screen</Text>
  </Screen>
));
